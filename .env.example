# Fire Enrich Environment Variables
# Copy this file to .env.local for local development

# Required: Firecrawl API Key
# Get yours at: https://www.firecrawl.dev/app/api-keys
# For Netlify: Add this in Site settings → Environment variables
FIRECRAWL_API_KEY=fc-your-api-key-here

# Required: OpenAI API Key
# Get yours at: https://platform.openai.com/api-keys
# For Netlify: Add this in Site settings → Environment variables
OPENAI_API_KEY=sk-your-api-key-here

# Optional: Enable unlimited mode
# When true, removes all limits on rows, columns, and file size
# Automatically enabled in development mode (NODE_ENV=development)
# For Netlify production: Set to "true" if you want unlimited mode in production
FIRE_ENRICH_UNLIMITED=false

# Node environment (automatically set by Netlify to "production")
# Do not set this manually in Netlify - it's handled automatically
NODE_ENV=development

# Netlify Deployment Notes:
# 1. Add FIRECRAWL_API_KEY and OPENAI_API_KEY in Netlify Dashboard
#    Site settings → Environment variables → Add variable
# 2. Set FIRE_ENRICH_UNLIMITED=true if you want unlimited mode in production
# 3. NODE_ENV is automatically set to "production" by Netlify during builds
# 4. Environment variables are available at both build-time and runtime
